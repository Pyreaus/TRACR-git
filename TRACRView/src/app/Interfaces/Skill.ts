export interface Skill {
    skillId: Number,
    localId?: string|null,
    skillName: string,
    skillDescription?: string|null,
    show?: boolean|null,
    colour?: string|null,
    Show?: boolean|null
}